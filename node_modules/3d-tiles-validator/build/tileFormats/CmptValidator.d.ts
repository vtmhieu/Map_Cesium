/// <reference types="node" />
import { ValidationContext } from "../validation/ValidationContext";
import { Validator } from "../validation/Validator";
/**
 * A class that can perform validation of CMPT data that is
 * given as a Buffer.
 *
 * @internal
 */
export declare class CmptValidator implements Validator<Buffer> {
    validateObject(uri: string, input: Buffer, context: ValidationContext): Promise<boolean>;
}
//# sourceMappingURL=CmptValidator.d.ts.map