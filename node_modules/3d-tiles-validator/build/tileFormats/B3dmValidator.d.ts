/// <reference types="node" />
import { ValidationContext } from "../validation/ValidationContext";
import { Validator } from "../validation/Validator";
/**
 * A class that can perform validation of B3DM data that is
 * given as a Buffer.
 *
 * @internal
 */
export declare class B3dmValidator implements Validator<Buffer> {
    validateObject(uri: string, input: Buffer, context: ValidationContext): Promise<boolean>;
}
//# sourceMappingURL=B3dmValidator.d.ts.map