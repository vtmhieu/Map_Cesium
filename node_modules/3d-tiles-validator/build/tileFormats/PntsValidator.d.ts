/// <reference types="node" />
import { ValidationContext } from "../validation/ValidationContext";
import { Validator } from "../validation/Validator";
/**
 * A class that can perform validation of PNTS data that is
 * given as a Buffer.
 *
 * @internal
 */
export declare class PntsValidator implements Validator<Buffer> {
    validateObject(uri: string, input: Buffer, context: ValidationContext): Promise<boolean>;
}
//# sourceMappingURL=PntsValidator.d.ts.map