/// <reference types="node" />
import { ValidationContext } from "../validation/ValidationContext";
import { Validator } from "../validation/Validator";
/**
 * A class that can perform validation of I3DM data that is
 * given as a Buffer.
 *
 * @internal
 */
export declare class I3dmValidator implements Validator<Buffer> {
    validateObject(uri: string, input: Buffer, context: ValidationContext): Promise<boolean>;
}
//# sourceMappingURL=I3dmValidator.d.ts.map